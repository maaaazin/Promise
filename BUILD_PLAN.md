# Build Plan — Deadline Radar (4.5hr window)

Living checklist. Update checkboxes and the STATUS line as phases complete — paste just this file back to me when asking "what's next," not the full history.

**Current phase:** Verification + hygiene pass conducted in a single continuous dev server session with full browser UI verification via `browser_subagent`. Seed extraction confirmed live (`[store] extraction: live — 6 commitment(s) extracted from 12 seed messages`). Manual message ingestion confirmed live (Riley Chen extracted and tracked, non-commitment message clean no-op). Clock advance by 3 days triggered live GPT-4o drafting for Jordan Ellis (62→77) and Maya (32→73) (`[drafts] draft: live`). In the browser UI, the EscalationCard rendered and was clicked for approval; the second queued card then appeared and was approved, both sending to Slack (`delivered: true`). Sidebar grounded Q&A answered accurately. However, the dev server console logged a `MissingToolResultsError` during the transition because `resolveNudge` called `appendMessage` for the queued nudge before `respond?.(...)` resolved the tool call. Per task constraints (no feature code changes, stop on any error), stopped before Step 2 / pre-push safety.

A 6th CopilotKit action, `ingestMessage`, is now live: it reuses `lib/extraction.ts`'s existing extraction function against a single manually-supplied message (no prompt/schema duplication), and on a real commitment (confidence ≥ 0.6) pushes a new tracked `Commitment` straight into the live in-memory store, so it's picked up by the very next `recompute()`/`advanceClock()` like any seed-derived commitment. Verified live: a real Slack-shaped message ("I will ship the quarterly report by Monday next week", mock-clock-relative) correctly produced a new commitment (Riley Chen, "Ship the quarterly report", due 2026-09-14), which then correctly went 31→67 on a subsequent 3-day clock advance alongside the unaffected seed commitments; a clearly non-commitment message ("great work on the demo yesterday") correctly added nothing. Exposed two ways as required: a `useCopilotAction` callable from the sidebar (wire-protocol-verified — `gpt-4o` correctly resolves a natural-language "ingest this Slack message from Alex: ..." into `ingestMessage({sourceType:"slack", rawText:"...", ...})`), and a minimal text-input + source-toggle + button form on the dashboard that calls the identical `/api/radar` `ingest` action directly, bypassing chat entirely. No writes to seed-*.json — new commitments live only in the runtime store, same lifetime/reset caveat as everything else there.

| # | Phase | Time budget | Done when | Status |
|---|---|---|---|---|
| 0 | Investigate existing repo | 15 min | Full audit report received | ✅ |
| 1 | Seed data + extraction pipeline (no UI) | 45 min | Running extraction against seed data returns correct Commitment[] in console/logs | ✅ verified live — `[store] extraction: live — 6 commitment(s) extracted from 12 seed messages`. All 6 of the unambiguous genuine-commitment messages produced a correct commitment (owner, description, and — checked by hand against each message's own timestamp — a correctly resolved due date, including "by Friday", "end of next week", and "next Wednesday" relative phrasing). The one deliberately vague message ("...before we ship, haven't forgotten") was correctly dropped. All 5 deliberate non-commitment messages were correctly excluded. Fallback path (no key) also independently confirmed clean in this same pass — see FAILURES.md. |
| 2 | Risk scoring + CopilotKit dashboard | 45 min | Dashboard renders real commitments with correct risk colors, useCopilotReadable exposes state | ✅ |
| 3 | Escalation drafting + HITL approval card | 45 min | Crossing threshold produces EscalationCard; approve/edit/dismiss all work | 🟡 verified in browser UI with queue progression and real Slack delivery (`delivered: true`), but server logged `MissingToolResultsError` due to `resolveNudge` triggering `appendMessage` before `respond?.(...)` completed (see FAILURES.md [14:41]). Needs tool-result ordering fix before push. |
| 4 | Slack webhook delivery | 30 min | Approved escalation lands in real Slack channel (or logs cleanly if webhook unset) | ✅ verified live this round with a real `SLACK_WEBHOOK_URL` — approving two different auto-flagged commitments both returned `delivered:true` from `lib/slack.ts`'s real `fetch()` call to the webhook (that code path only returns `delivered:true` after a genuine `response.ok`; a bad/missing webhook throws or returns `delivered:false` instead — no code change needed, `lib/slack.ts` was already correct). |
| 5 | Sidebar conversational queries | 30 min | Sidebar answers grounded questions about current state | ✅ verified — asked "What's my riskiest commitment right now?" through the real runtime with the sidebar's exact system instructions and real live state; the real `gpt-4o` answer correctly named Priya's partner-deck commitment at risk score 82, which was in fact the actual highest-scored commitment in the state at that moment. Verified via a direct protocol call rather than a rendered browser session (no browser tool available), but using the runtime's real request/response mechanics, not a mock. |
| 6 | Exa enrichment (stretch — cut first) | 20 min | Escalation draft includes fetched external context when relevant | ⬜ |
| 7 | Risk Radar visual polish | 20 min | Looks demo-ready, not just functional | ⬜ (explicitly out of scope for this task) |
| 8 | Manual message ingestion (`ingestMessage`, non-live-bot) | 30 min | Ingesting a real commitment-shaped message adds a new tracked commitment via both chat and a dashboard fallback form; non-commitments no-op; seed commitments unaffected | ✅ verified live — real commitment ingested and correctly participated in a later `advanceClock` recompute; non-commitment message correctly added nothing; sidebar-chat path wire-protocol-verified; dashboard form calls the same REST action directly as a non-chat fallback. No live Slack/email listener built — manual/pasted input only, per ARCHITECTURE.md's OAuth exclusion. |
| — | Buffer: demo rehearsal + record video | 20 min | Full 5-step demo script runs clean, under 2 min | ⬜ |

## Definition of done for the whole project
The 5-step demo script (dashboard calm → simulate time → risk spikes → draft appears → approve → Slack receives it → sidebar answers a grounded question) runs start to finish without a crash, using real API calls (not mocked responses), in under 2 minutes.

## Escalation rule for this plan
If any phase overruns its budget by more than 50%, stop, cut the next stretch item (see ARCHITECTURE.md cut order), and move on. Don't let one phase eat the buffer.
